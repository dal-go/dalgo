```yaml
feature: recordset-computed-columns
revision: e090c61
verdicts:
  - ac: recordset-computed-columns#ac:evaluator-compiles
    verdict: pass
    justification: "Type with Eval(map[string]any)(any,error) assigns to recordset.Evaluator; package builds and vets clean."
  - ac: recordset-computed-columns#ac:recordset-has-no-scripting-dependency
    verdict: pass
    justification: "go list -deps finds no starlark/goja/otto/cel/expr/lua/wasm engine; recordset imports are stdlib + roaring + testify only."
  - ac: recordset-computed-columns#ac:computed-column-registers-and-marks
    verdict: pass
    justification: "GetColumnByName('label') asserts to ComputedColumn with Evaluator()==e; stored 'qty' does not satisfy the assertion."
  - ac: recordset-computed-columns#ac:reject-set-on-computed
    verdict: pass
    justification: "SetValueByName/ByIndex error before delegating on a ComputedColumn; subsequent read returns the derived value, not the set value."
  - ac: recordset-computed-columns#ac:lazy-resolves-from-siblings
    verdict: pass
    justification: "GetValueByName routes ComputedColumn through resolveComputed; full-name example yields 'Ada Lovelace' with no error."
  - ac: recordset-computed-columns#ac:input-excludes-computed
    verdict: pass
    justification: "resolveComputed skips ComputedColumn siblings; recorded stored keys are exactly {first,last}, excluding full and greeting."
  - ac: recordset-computed-columns#ac:memoized-single-eval
    verdict: pass
    justification: "Per-row memo map evaluates once across 3 reads of the same Row instance; counter==1, all reads equal."
  - ac: recordset-computed-columns#ac:eval-error-propagates-and-caches
    verdict: pass
    justification: "(nil,err) outcome cached; second access returns the same error without re-invoking Eval, no panic, counter==1."
  - ac: recordset-computed-columns#ac:data-materializes-computed
    verdict: pass
    justification: "Row.Data routes ComputedColumn through resolveComputed; full column position holds 'Ada Lovelace', not a stored default."
  - ac: recordset-computed-columns#ac:returns-raw-uncoerced-value
    verdict: pass
    justification: "Resolution returns the evaluator value verbatim; int 42 comes back as int(42) with no coercion error."
```

# Verify Report — recordset-computed-columns @ e090c61

**Run revision:** e090c61
**Summary:** 10 passed, 0 failed, 0 unmapped, 0 errored

All 10 acceptance criteria are verified by commit `6ef832c` ("feat(recordset): add
computed columns with neutral evaluator contract"), whose `Verifies:` trailer lists
every AC. Each verdict below was produced by an independent verifier subagent that
fetched the commit diff and ran the package tests on demand.

## AC: evaluator-compiles

**Verdict:** pass

**Justification:** A type with method `Eval(stored map[string]any) (any, error)` assigns to a `recordset.Evaluator` variable; `go build ./recordset/` and `go vet` succeed.

**Commits:**
- 6ef832c — feat(recordset): add computed columns with neutral evaluator contract

**Evidence:**
- recordset/evaluator.go @ 6ef832c — `Eval(stored map[string]any) (value any, err error)`
- `go build ./recordset/` exit 0

## AC: recordset-has-no-scripting-dependency

**Verdict:** pass

**Justification:** `go list -deps ./recordset/` finds no scripting/expression engine in own or transitive imports; imports are stdlib, RoaringBitmap/roaring, and testify only.

**Commits:**
- 6ef832c — feat(recordset): add computed columns with neutral evaluator contract

**Evidence:**
- `go list -deps ./recordset/ | grep -iE 'starlark|goja|otto|cel|expr|lua|wasm'` → none
- recordset/evaluator.go, computed_column.go import only `fmt`/`reflect`
- recordset/computed_column_test.go uses `go/parser`+`go/token` solely to assert the absence

## AC: computed-column-registers-and-marks

**Verdict:** pass

**Justification:** `GetColumnByName("label")` asserts to `ComputedColumn` with `Evaluator()==e` (assert.Same); `GetColumnByName("qty")` fails the assertion.

**Commits:**
- 6ef832c — feat(recordset): add computed columns with neutral evaluator contract

**Evidence:**
- recordset/computed_column.go — `NewComputedColumn` returns `*computedColumn`; `var _ ComputedColumn = (*computedColumn)(nil)`
- recordset/computed_column_test.go — "registers-and-marks" subtest
- `go test ./recordset/ -run 'TestComputedColumn/registers-and-marks'` → PASS

## AC: reject-set-on-computed

**Verdict:** pass

**Justification:** `SetValueByName`/`SetValueByIndex` return an error before delegating when the column is a `ComputedColumn`; a subsequent read returns the evaluator-derived value, not the set value.

**Commits:**
- 6ef832c — feat(recordset): add computed columns with neutral evaluator contract

**Evidence:**
- recordset/recordset_columnar.go — `fmt.Errorf("cannot set value on computed column ...")` before `col.SetValue`
- recordset/computed_column_test.go — "reject-set-on-computed" asserts Set errors and read == "Ada Lovelace"

## AC: lazy-resolves-from-siblings

**Verdict:** pass

**Justification:** `GetValueByName` routes a `ComputedColumn` through `resolveComputed`, which builds the sibling map and calls `Eval`; the example returns `"Ada Lovelace"` with no error.

**Commits:**
- 6ef832c — feat(recordset): add computed columns with neutral evaluator contract

**Evidence:**
- recordset/recordset_columnar.go — `GetValueByName` → `resolveComputed` → `cc.Evaluator().Eval(stored)`
- `go test ./recordset/ -run 'TestComputedColumn/lazy-resolves-from-siblings'` → PASS

## AC: input-excludes-computed

**Verdict:** pass

**Justification:** `resolveComputed` skips `ComputedColumn` siblings when building the stored map; recorded keys are exactly `{first,last}`, excluding both `full` and `greeting`.

**Commits:**
- 6ef832c — feat(recordset): add computed columns with neutral evaluator contract

**Evidence:**
- recordset/recordset_columnar.go — `if _, isComputed := col.(ComputedColumn); isComputed { continue }`
- recordset/computed_column_test.go — `assert.ElementsMatch([]string{"first","last"}, recorded keys)`

## AC: memoized-single-eval

**Verdict:** pass

**Justification:** Per-`columnarRow` memo map evaluates once across three reads of the same `Row` instance; counter==1 and all three reads equal. (GetRow(i) returns a fresh row each call; the test reuses one instance.)

**Commits:**
- 6ef832c — feat(recordset): add computed columns with neutral evaluator contract

**Evidence:**
- recordset/recordset_columnar.go — `resolveComputed` memoizes via `row.memo` keyed by column name
- recordset/computed_column_test.go — "memoized-single-eval" reuses `row := rs.GetRow(0)`; asserts counter==1

## AC: eval-error-propagates-and-caches

**Verdict:** pass

**Justification:** The `(nil, err)` outcome is cached; a second access returns the same error without re-invoking `Eval`, with no panic; counter==1.

**Commits:**
- 6ef832c — feat(recordset): add computed columns with neutral evaluator contract

**Evidence:**
- recordset/recordset_columnar.go — memo cache returns `cached.v, cached.err`
- recordset/computed_column_test.go — "eval-error-propagates-and-caches" asserts ErrorIs(boom) twice, nil values, calls==1

## AC: data-materializes-computed

**Verdict:** pass

**Justification:** `Row.Data(rs)` routes computed columns through `resolveComputed`; the `full` column position holds `"Ada Lovelace"`, not a stored-cell default.

**Commits:**
- 6ef832c — feat(recordset): add computed columns with neutral evaluator contract

**Evidence:**
- recordset/recordset_columnar.go — `Data` calls `row.resolveComputed(cc, rs)` for `ComputedColumn`
- recordset/computed_column_test.go — "data-materializes-computed" asserts `data[fullIdx] == "Ada Lovelace"`

## AC: returns-raw-uncoerced-value

**Verdict:** pass

**Justification:** The resolution path returns the evaluator's value verbatim with no coercion; an `int` `42` comes back as `int(42)` with no error.

**Commits:**
- 6ef832c — feat(recordset): add computed columns with neutral evaluator contract

**Evidence:**
- recordset/recordset_columnar.go — `resolveComputed` returns `Eval(stored)` result unchanged
- recordset/computed_column_test.go — "returns-raw-uncoerced-value" asserts `Equal(42, val)`, no error
